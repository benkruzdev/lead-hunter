// SearchIntelligenceBar — metrics removed (cache/cost/freshness carry no real value at this stage)
export interface SearchIntelligenceBarProps {
    cost?: number;
    className?: string;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function SearchIntelligenceBar(_props: SearchIntelligenceBarProps) {
    return null;
}
