# Development Environment Variables
# Copy this to .env.local and fill in your actual values

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend API URL
# For local development:
VITE_API_URL=http://localhost:3001
# For production (after deploying to Render):
# VITE_API_URL=https://leadhunter-api.onrender.com

# Admin Route Security (SPEC 6.1)
# REQUIRED: Secret path segment for admin routes
# Must match ADMIN_ROUTE_SECRET in backend .env
# Example: VITE_ADMIN_ROUTE_SECRET=my-secret-admin-path-2024
VITE_ADMIN_ROUTE_SECRET=your-admin-route-secret-here

# ========================================
# THIRD-PARTY INTEGRATION KEYS
# ========================================
# IMPORTANT: In production, these keys are configured via Admin Panel.
# For local development only, you can set them here temporarily.
# Leave empty if not testing these specific features.

# reCAPTCHA v3 (Optional - for testing registration)
# Get from https://www.google.com/recaptcha/admin
# VITE_RECAPTCHA_SITE_KEY=

# Google OAuth is configured in Supabase Dashboard
# (Authentication > Providers > Google)
